# atom-select-list (Chevron)

**Required export:** `SelectListView` constructor.

Props Chevron / bundled packages pass: `items`, `elementForItem`,
`didConfirmSelection`, `didCancelSelection`, `filterKeyForItem`,
`emptyMessage`, `errorMessage`, `infoMessage`, `skipCommandsRegistration`,
and optional `initiallyVisibleItemCount`.

Root was `^0.8.1`; several owned packages nested `0.7.2`. Chevron pins 0.8.1
everywhere. 0.8 is backward-compatible for 0.7 constructor props.

Ships compiled `lib/` from the npm 0.8.1 tarball (no v0.8.1 git tag).
